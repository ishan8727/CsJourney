1. npm init -y
2. cargo.toml
3. ~ requirements.txt

config files.

name, version, desciption
=> maily used for external work 
    eg: deploying packages to npm registety - defining versions of product etc.

scripts: bunch of commands.
=> npm run scriptName

=> metadata: mainly when deploying packages to npm registety

dependies sections as well.

package.lock.json:
=> locks the versions of the dependencies used in the project
so that if multiple devs on a project - project remain consistent. 
